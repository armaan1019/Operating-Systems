I observed that when I run the sample program, it only runs if my username is the username of the account im in. It waits for an input and calculates the area after I enter the width and height of a rectangle. HOME, USER, PATH, PWD, all are variables stored by your computer. `` executes a command and replaces the `` with the output. wc -l gets the line count so when `who | wc -l` runs, it will get the number of lines of the file that contains all the users logged in, hence the number of users. The first echo command will get the total number of files. bin/ls will local search and then wc -l will get the total number of lines. The second echo command will count the lines in each file.
