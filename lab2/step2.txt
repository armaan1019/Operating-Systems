My observations from this program are that fork() created a child process. Pid is used to controll which process does what. Only the parent process will execute the first if and only the child will execute the else. Usleep is used to set the delay.
